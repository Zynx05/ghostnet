# Copy this to .env and change it if your database is somewhere else.
# The format is postgresql://user:password@host:port/database
DATABASE_URL=postgresql://ghostnet:ghostnet@localhost:5432/ghostnet
